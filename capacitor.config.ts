import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Trabalhando_Com_GitHub',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
